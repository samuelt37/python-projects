 PATCH (cont�d)<body>

  PATCH (cont�d)  
      Since semantics of PATCH are non-idempotent, responses to PATCH are not cacheable
 
     If the PATCH request appears (at least initially) to be acceptable, the server MUST transmit an interim 100 response message after receiving the empty line terminating the request headers, and continue processing 
     
 
     Previous slide   
     Next slide   
     Back to first slide   
     View graphic version   
  </body> 