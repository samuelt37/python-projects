 ### R functions for
### A Bayesian Discovery Procedure
### by Guindani, Muller, Zhang

library(MCMCpack)
library(mvtnorm)
library(Matrix)


## MCMC function
## for the analysis in section 5.2

cDPODP 1)
          {
            if (sum(label==j0)==0) # lonely guy
              label  j0, label-1, label)
          }
        L 1){
      ## posterior predictive parameters
      post.pred.var[2:L] 1)
          {
            if (sum(label==j0)==0) # lonely guy
              label  j0, label-1, label)
          }
        L 5) browser()
    L 1){
      for(j in 2:L){
        q[j]  